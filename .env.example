# qcontrol — dev defaults.
# On the VPS, copy to .env and replace QCONTROL_TOKEN with a long random string.
# Generate one: openssl rand -hex 32

QCONTROL_TOKEN=replace-me-with-a-long-random-string
QCONTROL_PORT=8089
